programa
    declare a,b,c,d;
    escreva("Meu primeiro programa em CU Soh CU eh universal");
    ||leia( a );
    leia( b );
    d := 1 + 2;
    ||c := a + b;
    escreva("O resultado da soma vale");
    escreva( c );
fimprog
